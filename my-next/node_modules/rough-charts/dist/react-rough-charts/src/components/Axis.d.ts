import * as React from 'react';
import { ChartContextProps } from './ChartContext';
export interface AxisProps extends ChartContextProps {
}
export declare const Axis: React.FC<AxisProps>;
export default Axis;
