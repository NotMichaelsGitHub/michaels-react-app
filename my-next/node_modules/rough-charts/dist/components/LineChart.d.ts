import * as React from 'react';
import { BaseChartProps } from '../baseTypes';
export interface LineChartProps<T = any> extends BaseChartProps<T> {
    children?: React.ReactNode;
}
export declare const LineChart: React.FunctionComponent<LineChartProps<any>>;
export default LineChart;
