import * as React from 'react';
import { ScaleBand } from 'd3-scale';
import { BaseChartComponentProps } from '../baseTypes';
export interface AxisProps extends BaseChartComponentProps {
    dataKey: string;
    tickSize?: number;
    fontSize?: number;
    scale?: ScaleBand<any>;
}
export declare const Axis: React.FC<AxisProps>;
export default Axis;
